package com.valoser.futaburakari.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Material 3 template color swatches.
 * These values are not wired into the current `FutaburakariTheme` color schemes
 * (see `Theme.kt`, which defines explicit schemes per mode). They remain here as
 * convenience swatches for future styling or component-level overrides.
 */
val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

// Darker counterparts used in the default template
val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)
