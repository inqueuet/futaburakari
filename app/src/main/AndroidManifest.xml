<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" />

    <!-- For Android 13 (API 33) and above -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <!-- For Android 14 (API 34) and above: allow access to only user-selected media -->
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />
    <!-- For Android 12 (API 32) and below -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />

    <!-- Remains for older devices, if still needed for other purposes -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Futaburakari"
        android:networkSecurityConfig="@xml/network_security_config"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher">
        <!-- AdMob App ID (test). Replace for production. -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="@string/admob_app_id" />
        <!-- WorkManager uses Configuration.Provider in MyApplication for setup -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            tools:node="merge">
            <meta-data
                android:name="com.valoser.futaburakari.init.OkHttpPublicSuffixInitializer"
                android:value="androidx.startup" />
        </provider>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Futaburakari">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".permissions.MediaPermissionActivity"
            android:exported="false"
            android:theme="@style/Theme.Futaburakari" />
        <activity android:name=".DetailActivity" />
        <activity
            android:name=".ReplyActivity"
            android:launchMode="singleTop"
            android:label="レスを投稿"
            android:parentActivityName=".DetailActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".BookmarkActivity"
            android:label="ブックマーク管理"
            android:parentActivityName=".MainActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".ImagePickerActivity"
            android:label="Select Image" />
        <activity
            android:name=".ImageEditActivity"
            android:label="Edit Image" />
        <activity android:name=".ImageDisplayActivity" />
        <activity
            android:name=".MediaViewActivity"
            android:exported="false"
            android:theme="@style/Theme.Futaburakari">
        </activity>
        <activity android:name=".SettingsActivity"
            android:label="設定" />
        <activity android:name=".HistoryActivity"
            android:label="@string/history_title" />
    </application>

</manifest>
